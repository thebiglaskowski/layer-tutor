{
  "name": "layer-tutor",
  "private": true,
  "type": "module",
  "description": "Typing tutor for a layered QMK split keyboard",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:layout": "node ../scripts/check-layout.mjs",
    "serve": "python3 -m http.server 8000",
    "deploy": "bash ../scripts/deploy-pages.sh"
  }
}
